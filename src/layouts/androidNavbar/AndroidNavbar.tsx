import styled from "./androidNavbar.module.scss";

export const AndroidNavbar = () => {
    return (
        <div className={styled.and_navbar}>
            <div className={styled.buttons}></div>
        </div>
    )
}